# M1 findings: NTSC-U 1.1

- Disc volume: `SCUS94240`.
- Main executable: `SCUS_942.40`, 1,800,192 bytes.
- Load range: `0x80010000` through `0x801C7000`.
- Entry point: `0x800E3DA4`.
- Executable SHA-256: `bac292061ad5bc718ce137ef5b43d3d7e9b1b65248fb0d52229f328ccfe4ab4e`.
- Executable overlays: 18 files under `BIN`.
- Direct-call seed map: generated by `sf_tool map-functions`; it is intentionally conservative and must be refined by control-flow analysis.
- PS-X runtime entry `0x800E3DA4` calls the game `main` at `0x8001457C`.
- Full analysis baseline: Ghidra 12.1.2 plus `ghidra_psx_ldr` 2026.07.08.
- Ghidra recovered 2,152 functions in the main load range: 278 PsyQ/library symbols and 1,117 still-unidentified game functions. The remaining named internal object routines belong to matched PsyQ objects.
- The first native recovery covers `main` plus the state push/pop behavior at `0x80016020` and `0x80016094`.
- `TITLE.OVL` loads raw at `0x80146630`; its verified per-frame state entry is `0x80149CF4`.
- The title overlay analysis currently recovers 24 functions from that verified entry.

Preserved source-path strings expose these original modules: `system`, `game`, `path`, `object`, `target`, `intrface`, `particle`, `sound`, `gtegeo`, `terrain`, `stream`, `text`, `pickup`, `save`, `egsdisp`, `egshmd`, `egsdev`, `egsanim`, `egstex`, `egsmem`, `egsfile`, `egshog`, `cas`, and `npc`.
