# Industry Bold source font

Place the user-supplied `Industry-Bold_RUS.ttf` in this directory before
regenerating the Russian font atlas. The TTF is ignored by Git and is never
copied into release packages.

The checked-in TIM files under `assets/locales/ru-vit/fonts/` are the generated
runtime assets.
